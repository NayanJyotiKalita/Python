# Not even

Complete the code snippet given below to remove all the elements in the list that are even and print the new list in which there are no even elements. </br>

**Input:** elements=[24,32,13,15,19,44,52,7,9,2,51]

```python
def not_even(lst):
  res = []
  for ele in lst:
    ________________
  return res

not_even(elements)
```
Required output:[13, 15, 19, 7, 9, 51]

---

## Choose the correct answer from below:

  - if (ele % 2 != 0): res.append(ele)

  - if (ele% 2 == 1): lst.remove(ele)

  - if (ele% 2 == 1): lst.pop(ele)

  - if (ele% 2 != 0): res.pop(ele)


--- 

# Answer
*if (ele % 2 != 0): res.append(ele)*
